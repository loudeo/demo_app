<p id="notice"><%= notice %></p>

<h1>Список клиентов</h1>

<table>
  <thead>
    <tr>
      <th>Имя</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= link_to 'Показать', user %></td>
        <td><%= link_to 'Править', edit_user_path(user) %></td>
        <td><%= link_to 'Удалить', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Новый клиент', new_user_path %>
